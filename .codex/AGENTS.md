# JavaScript and TypeScript

- Strongly prefer Bun as the JavaScript/TypeScript runtime and package manager whenever possible. Use `bun`, `bun run`, `bunx`, and `bun install` instead of Node.js, npm, npx, Yarn, or pnpm unless project constraints require otherwise.
